// Features of these components:
// - They are all functional components.
// -
// gridColumn / gridRow	Locks layout in place — no visual shifting
// zIndex	Controls visibility stack, independent of DOM order
// return null;	Cleanly removes component when hidden
// isVisible	Smart prop to toggle visibility in layout-safe way

// “These components will all handle, its own logic and state except when passing or receiving global state and variables”